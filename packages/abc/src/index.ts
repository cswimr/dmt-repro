/**
 * Log the input string to the console.
 * @param input - The string to log.
 * @example abc('Hello, world!') // Logs 'Hello, world!' to the console
 */
export default function abc(input: string) {
  console.log(input);
}
